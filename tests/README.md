# Tests

`encos_protocol_test.c` verifies the 86/92-byte PDO structure sizes, discovery and query packet encoding, reply decoding, endianness, DLC validation, and motor error extraction. Run it through `./scripts/build.sh`.
